# Backlog

**進場規則：**要放東西進來，必須講得出它擠掉了什麼。沒有排擠就沒有選擇。

**已知的結構偏差（2026-09-05 review）：**進場規則要求「講得出擠掉了什麼」，
而 `[ENG]` 項目不回答產品問題，所以在 `[L1]` 面前永遠輸——三輪 sprint 下來
ENG 進 DO 的次數是 **0**。這不是優先序判斷的結果，是規則的副作用。
處理方式見 sprint 003 spec：DO 固定保留一格給 ENG 或「使用」。

**過期規則：**超過三個月沒動的項目提議**刪除**，不是重排優先序。
刪是免費的——真的重要它會自己再冒出來，而那就是它值得做的證明。

層次標記：`[L1]` 數學層（純量價）、`[L2]` 故事層（脈絡、敘事）、`[ENG]` 工程品質。

---

## 待排

| 項目 | 層次 | 來源 | 備註 |
|---|---|---|---|
| 資料窗已拉長；**as-of 成分股**仍缺 | L1 | 2026-09-05 review | 回補與對齊樣本在 003/004 做了；D6／Q5 的 as-of 成員尚未做。單獨「再回補」不進 DO |
| `arch.bootstrap` 取代手刻循環位移 | L1 | 2026-09-05 research | `CircularBlockBootstrap` / `StationaryBootstrap` + `optimal_block_length()`。現在的 k=20 是寫死的單一參數；block bootstrap 給信賴區間而非單一百分位。不模擬損益，不碰 R1/D2。NCSA 授權。二選一，別跟 `recombinator` 都裝（D9）。**010 研究查證：8.0.0／2025-10-21 發布／仍在維護**，可行；押後的理由是它動 compute 層＋新增相依，與 010 Appetite 衝突，不是不可行 |
| 市場層級 breadth | L1 | 2026-09-04 研究 | 現有 breadth 是族群屬性；市場層級的版本提供「這次輪動發生在寬廣還是狹窄的市場」的解讀脈絡 |
| 除權息幅度量測 | L1 | 2026-09-04 討論 | 七八月除權息集中，未還原收盤價使高殖利率族群被系統性壓低。**先量幅度**（§8 允許 report），改方法需先改 §8 |
| 個股層排序與呈現檢視 | L1 | 2026-09-04 | `compute_stock_metrics()` 已算好個股 RS20 與 Leader/Follower/Laggard，可能只需調整呈現 |
| `cli.py` 覆蓋仍薄 | ENG | 2026-09-04 技術債 | 已有 `test_cli_smoke.py`（analyze→brief→radar）；子命令級覆蓋仍不足 |
| 無 lint / 型別檢查 / CI | ENG | 2026-09-04 技術債盤點 | `.gitignore` 已列 `.ruff_cache/`、`.mypy_cache/`，工具從未接上 |
| `missing_count` 純量迴圈 | ENG | 2026-09-04 技術債盤點 | `calc.py:233–243`，可向量化 |
| product.py / radar.py 重複的格式化層 | ENG | 2026-09-04 技術債盤點 | 十個 helper 各自處理 n/a，四種不同的 sentinel。建議在下次動到任一 renderer 時順手抽出 |
| `radar.py` 180 行 f-string HTML | ENG | 2026-09-04 技術債盤點 | 含內嵌 CSS，且插值未跳脫。建議拆出 `radar.css` 並過 `html.escape()` |
| ~~radar 表格 `Momentum` 欄沒有欄寬~~ **→ 012 DO-1** | ENG | 009 驗收・**011 惡化** | 009 DO-3.1 只給 `敘事` 補了右側空白（最後一欄，畫面零改變），真正的病灶是 `Momentum` 標籤長度 4–9 讓 `敘事` 欄左緣浮動。**011 DO-3 把那一格從 4–9 格拉到 22–27 格**（`radar.py:154-163` `_fmt_mom_ascii` 無欄寬），`敘事` 左緣浮動再加 18 格位移，`"-" * _vislen(header)` 分隔線也短於資料列。物證見 `011-report.md` 第 4 節。**排 012 第一格** |
| `scripts/acceptance-check.sh` 驗收機械項自動化 | ENG | 2026-09-06 skill retro | 規劃端每輪手工重跑同一組 grep：釘住的函式 diff 是否為空、測試名有無目視宣稱詞、evidence 有無真的 `N passed in Ns` 行、每個驗收條件代號有無對應輸出。**「逐字元相同」這條驗收句型連續出現在 003–010 共 8 份 spec**。官方判準：必須每次發生、不需判斷 → 確定性腳本／hook，不是 skill、不是靠模型自律 |
| radar 新鮮度改成「距今差幾個交易日」 | 使用 | 010 驗收 | **011 降級為 UNKNOWN。****立案物證是錯的**——010-report 寫「09-05 週五是交易日」，但 2026-09-04 是週五、09-05 是週六，當天資料並未落後（`011-evidence.md` 第 1 節）。`caught_up` 偵測不到「根本沒跑過 refresh」這個結構性論點仍成立，但沒有真實案例；且研究證實 `pandas_market_calendars` 無台灣日曆。**更正（2026-09-07）：那句話只對 `pandas_market_calendars` 成立，當時漏了兩條路——TWSE 官方 `holidaySchedule` JSON、以及 `exchange_calendars` 的 `XTAI`。012 DO-3 已採前者，技術前提消失，剩下的只有「沒有真實案例」那個問題。**回頭日期：012 驗收後**——屆時 011 DO-2 已讓「這一天到底發生什麼」可區分，再決定要不要做 |
| `DISPERSION_PCT_DIRECTION` 措辭「名次越擠」不準 | 使用 | 010 驗收 | `dispersion` 是 top-half 減 bottom-half 的平均 RS20（`quality.py:71`），量的是強弱差距、單位百分點；名次永遠 1..11 不會擠。改成「族群之間的強弱差距越小」。一行 |
| momentum 標籤重做，且必須可還原 | 使用 | 010 拍板 4 | D16 收窄後的第一個受約束項目：讀者要能從同一列可見數字還原出標籤，否則改成不顯示。物證見 `010-report.md` 第 6 節 |
| gazetteer（`pyahocorasick` ＋ 凍結 ISIN 表）、`narrate add` | L2 | 009 spec 明確押後 | 009 Appetite 把它們推到 010：新相依＋新資料＋第二層寫入端 |
| `data/raw` 可驗證清單（session date・校驗和・判定：交易日／假日／抓取失敗）**—— 011 驗收後由「好題目」升格為前置** | ENG | 011 階段 1 ＋ 011 驗收 | 836MB／882 檔、`.gitignore` 排除、無備份、不可重新產生。**升格理由：**011 DO-2 的「官方休市 vs 抓取失敗」判定掛在**檔案 mtime**（`data.py:357-367`）——32 個「官方休市」全部來自 2026-08-31 那次批次回補留下的 mtime，一次 `cp -r`／備份還原就會同時失真，而且不會出聲。現在有一個功能依賴它了 |
| §27 的「月份 → 當月贏家」分段序列圖 | L1 | 011 階段 1 | §27 的成功判準指的就是這張圖。資料窗已從 161 天長到 441 天，畫面最長的時間軸卻還是 drill-down 的 20 個 session。純讀既有 parquet，不新增統計量 |
| `normalize_all` 每次 refresh 全量重解 836MB | ENG | 011 階段 1 | `cli.py:272` → `data.py:513-521`，對全部 882 個 session 逐一 `json.loads` + `concat` 再整份重寫 parquet。加一天要付全量代價 |
| TWSE payload 沒有日期對帳（TPEx 有） | ENG | 011 階段 1 | `parse_tpex_payload:237-243` 會比對 `payload["date"]` 並 raise；`parse_twse_payload:184-188` 只在 `session is None` 時讀，而 `load_raw_session:494` 永遠傳檔名推出的日期。檔案裝錯天的資料會全程不 raise |
| `date.today()` 是 UTC，TWSE session 是台北時間 | ENG | 011 階段 1 | `cli.py:615`、`data.py:425/468`。台北 00:00–08:00 期間兩者差一天。2026-09-01 那個空檔幾乎確定是這條路徑的產物（mtime `2026-08-31 16:30 UTC` = 台北 09-01 00:30）。011 DO-2 會碰到它 |
| 除權息幅度量測（能看見 3–8% 的那種） | L1 | 011 驗收 | **011 DO-1 用錯了尺。**±10% 只看得到災難級斷點（主題成員全樣本 3 筆），而除權息幅度多在 3–8%、全部在門檻以下。TWSE 有官方免費「除權除息計算結果表」可日期區間查詢（<https://www.twse.com.tw/zh/announcement/ex-right/twt49u.html>）。§8／Q3 仍只允許量測回報，不改價格方法 |
| ~~A4 那一行改用實際漲跌停價比對~~ **→ 012 DO-2** | 使用 | 011 驗收 | 現在讀作「近 20 個交易日 105 筆」，其中絕大多數是 ±10.0% 的漲跌停檔位進位（全樣本 1644/1909 = 86%），6669 被埋在裡面。**檔位表也是交易所規則，不是 D10 把手**——用規則的完整形式取代 0.10 這個簡化版 |
| 窮舉取代虛無抽樣（006-review A1） | ENG | 011 驗收・**現在有物證了** | 多一個交易日讓 k=1 的距離從 +34.36σ 跳到 +39.41σ，觀測值一動未動，位移全部來自 `null_std` 0.0254 → 0.0221（−13%）。1000 抽樣對標準差的估計誤差約 2%，所以這不是抽樣雜訊。`CLAUDE.md` 把 σ 印到小數點後兩位，而它在一天的擾動下動 15%。物證見 `011-report.md` 第 5 節 |
| `impossible_daily_returns` 的 prev close 會跨過停牌空窗 | ENG | 011 驗收 | `groupby("symbol").shift(1)` 取的是上一筆有資料的日，個股停牌時多日報酬會被印成單日。265 筆「真的超出漲跌停」裡有多少是這樣來的，規劃端算不出來 |

### Sprint 012 已排入（2026-09-07；待實作）

**本輪主題：用交易所自己的算術，不要用它的比率近似。**第一層的**輸入**正確性，
延續 011。第二層完全不碰。

| 項目 | 層次 | 備註 |
|---|---|---|
| `Momentum` 欄補欄寬 | 使用 | 012 DO-1。009 舊項、011 DO-3 惡化（4–9 格 → 22–27 格），`radar.py:230` 表頭字面無寬度、`radar.py:258` 分隔線用 `_vislen(header)` |
| `LIMIT_MOVE = 0.10` 換成交易所漲跌停價（檔位表＋十進位比較） | L1・買資訊 | 012 DO-2。**併掉舊的「A4 改用實際漲跌停價」那一列。**成因更正見下 |
| 休市判定改讀 TWSE 官方休市表，mtime 降為最後手段 | ENG | 012 DO-3，**本輪 ENG 保留格**。拆掉 011 DO-2 的 mtime 依賴；官方表只有排定休市，颱風臨時停市（2026-07-10）仍是未知，但範圍從 32 天縮小 |

**更正（2026-09-07，規劃端）：011 那 1640 筆偽陽性的成因寫錯了。**
011-report §2.1 與 `calc.py:41-43` 都寫「漲跌停 × 檔位進位」，但漲停價無條件捨去、
跌停價無條件進位，之後的比率只會 ≤ 10%，**檔位進位不可能製造超過 10% 的比率**。
真正的成因是拿浮點比率去比十進位門檻（`abs(44.55/49.5 - 1) > 0.10` → `True`）。
物證與三筆回算見 `012-evidence.md` §0.1。**這是規劃端在 011 spec 先寫下的錯誤歸因。**

### 橫斷面動能指標盤點（2026-09-04 評估，結論：不新增）

社群與開源常見的動能指標盤點結果。**記錄在此是為了避免日後重複討論。**

| 指標 | 判定 | 理由 |
|---|---|---|
| KD / 隨機指標 | **不適用** | 單一標的的時序震盪指標，量的是「價格在自己近期區間的哪個位置」。輪動是橫斷面問題——兩個主題可以同時 KD 80 而相對大盤一個 +20% 一個 +2%。另有 (9,3,3) 參數與 20/80 門檻，全是 D10 要擋的可調把手 |
| RSC（Relative Strength Comparative） | **已具備** | 標的除以基準的比值線。`rs20` 本質上就是它的窗口版本 |
| Mansfield RS（Weinstein Stage Analysis） | **已涵蓋** | RSC 再對自身移動平均正規化，回答「相對強弱本身在不在改善」。`rank_delta_5` 已在名次空間涵蓋此維度 |
| RRG 的 RS-Ratio / RS-Momentum | **non-goal D3** | `rank` + `rank_delta` 已涵蓋雙軸概念的實用部分 |
| RSI 作為動能選股因子 | **不適用** | 那是策略不是指標，驗證需要完整回測（D2） |

**結論：橫斷面輪動的既有工具箱本來就小，而主要那一項已經在用。**
散戶熟悉的指標（KD、RSI、MACD、布林）幾乎都是單一標的的時序工具。

**更重要的推論：**sprint 001 發現 20 日尺度上結構不穩，那是**市場的性質，不是量測的缺口**。
若一個月前的名次對今天沒有解釋力，再加一個震盪指標也不會讓它變得有解釋力。
正確反應是知道它並照著讀（短端信、長端打折），不是找新指標把它填滿。

**2026-09-06 再確認（不重開）：**市面／OSS 主流仍是相對報酬→橫斷面排名（多窗
composite、IBD RS Rating、12−1、residual momentum、Elo 皆然）。004 的 σ 梯度
（k=1/+34.4σ → k=5/+25.4σ → k=20/+2.84σ）強化「問題在尺度不在統計量」。
除非 as-of 成分重測翻轉 k=20 讀法，否則不重開「換 rank」討論。sprint 005 改買
前向 Rank-IC 物證，不換 primary。

### 開發流程要不要再加 skill（2026-09-06 評估，結論：不加）

**記錄在此是為了避免日後重複討論。**

官方判準（<https://code.claude.com/docs/en/skills>）：
「當你一直複製貼上同一套指令／checklist，或 `CLAUDE.md` 裡某段已經從**事實**長成
**程序**時，才該轉成 skill。」另建議 `SKILL.md` 保持在 500 行以下。

| 候選 | 判定 | 理由 |
|---|---|---|
| 實作端專屬 skill | **不加** | `CLAUDE.md` 的「Working a Sprint Spec」的確是程序不是事實，符合官方的轉換訊號**的一半**——但另一半是「只有時才相關」。這個 repo 每一次 session 都是 sprint 工作，所以它永遠相關，留在常駐檔裡是對的 |
| 引用 `engineering:*` 內建 skill（code-review／tech-debt／testing-strategy／architecture） | **不引用** | 四個都跟現有機制正面重疊：驗收＝sprint skill 階段 0；技術債＝backlog 的 ENG 保留格；最小測試清單＝contract §9；ADR＝規則帳（且規則帳多了「重看日期」）。引進來會製造第二個真理來源，而 `CLAUDE.md` 明文規定 source of truth 的順序 |
| 收工前強制跑 `pytest` 的 Stop hook | **不加** | 官方確實建議「必須每次發生」的事用 Stop hook（exit 2 擋住收工）。但**起因填不出來**：000–010 每一份 evidence 都有真實的 `N passed in Ns` 行，沒有一輪漏跑或謊報。按本專案自己的標準，沒有物證就不加規則 |
| 把驗收的機械檢查寫成腳本 | **加**，見待排 | 這一項有物證（8 份 spec 同一句型、每輪手工重跑同一組 grep），且屬「必須每次發生、不需判斷」，官方判準指向確定性腳本而非 skill |
| 拆 `marketpulse-sprint` skill 成多檔（progressive disclosure） | **先不拆，標觸發條件** | 現在 389 行／官方上限 500 行。**觸發條件：**哪一輪出現「skill 裡有一條沒被執行，因為讀的人漏掉了」，那時再把階段 4 的 spec 樣板與規則帳慣例移進 `references/` |

**現況尺寸（2026-09-06）：**`CLAUDE.md` 344 行、`docs/coding-contract.md` 310 行、
`SKILL.md` 389 行。官方對「過度囉唆的 `CLAUDE.md`」的建議是**修剪或轉成 hook**，
不是升級成 skill——記在這裡，下次覺得「該再寫一份文件」時先回頭看這一行。

### ~~Sprint 004 已排入~~（2026-09-06 已併入 `dev@42c3522`）

| 項目 | 層次 | 備註 |
|---|---|---|
| narrative schema：`branches` / `log` / `stage` / `revisit` | L2 | 004 DO-1。擠掉的是「資料窗回補」——回補綁著 D6，是更貴的一輪 |
| 支線籃子強弱面板（並列、不排名、不進主排名池） | L2 | 004 DO-2。繞開 Q6 而非回答它 |
| `calc.py` 資料空窗檢查 | ENG | 004 DO-3。**第一次有 ENG 項目進 DO**，靠的是它同時是 DO-2 的前置，不是靠優先序判斷 |

### Sprint 005 已排入（2026-09-06）

| 項目 | 層次 | 備註 |
|---|---|---|
| 前向 Rank-IC 3×3（`rank-ic`） | L1 | 買資訊；不改 primary |
| `above_count` 全 NaN → NaN | L1 | 邊界假 0 |
| Brief／radar B＋C 固定短註 | 使用 | 字面鎖定 |

### Sprint 010 已排入（2026-09-06；已於同日併入 `dev@874302d`，三項全數通過）

**本輪主題：已經算出來、但沒有送到畫面上的東西。**三項全部是顯示層，零新計算、
零新相依、零新資料；硬邊界是 `theme_daily.parquet` 與 `market_daily.parquet` 前後不變。

| 項目 | 層次 | 備註 |
|---|---|---|
| 品質行改寫成讀得懂的樣子（含 `entry is None` 改印「無基準」） | 使用 | 010 DO-1。`_percentile` 是 `rolling(60)`，所以 `離散 …(18%)` 早就在說「今天名次差異資訊量偏低」，只是沒印出來。擠掉的是「兩兩相關係數新指標」——研究證明那是同一個構念 |
| `radar.html` 標頭補新鮮度（複用 `format_ops_status`） | ENG | 010 DO-2，**本輪的 ENG 保留格**。`cli.py:212` 已算好 `caught_up`，只印在終端機 |
| 2×2 的「被講過」定義與 `敘事` 欄統一；`尚未生效`／`最近事件` 補溢出提示 | L2 | 010 DO-3。`cli.py:201` 已存 `mention_dates`，但 `product.py:231` 忽略它。**今天可見效果為零**，差異要到某主題被講過又掉出榜才出現 |

### Sprint 011 已排入（2026-09-06；2026-09-07 三項全數通過，已併入 `dev@4225531`）

**本輪主題：資料在說謊的時候，畫面要出聲。**第一層的**輸入**正確性優先於呈現。

| 項目 | 層次 | 備註 |
|---|---|---|
| 找出並指名「數學上不可能的單日報酬」 | L1・買資訊 | 011 DO-1。6669 緯穎 08-31 收 7,095 → 09-02 收 2,610，一夜 −63%；五個交易日下限是 −41%。它把 AI伺服器族群打到第 11 名。§8／Q3 已決：只量測回報，不改價格方法 |
| 「官方休市」與「抓取失敗」不再長得一樣 | ENG | 011 DO-2，**本輪 ENG 保留格**。`data.py:395-398` 一律回 `holiday`、`data.py:366-372` 永不重抓 |
| Momentum 可還原，否則不顯示；離散措辭 | 使用 | 011 DO-3。D16 的重看點。`MomentumEvidence` 早就帶四個方向，主表那一格只印標籤 |

### 脈絡挖掘（2026-09-04 提出；burst／PTT 已於 007 刪除）

前提：sprint 001 的 UNKNOWN「每週手動記錄撐不撐得住四週」要先有答案。
自動化的價值完全取決於它。

**2026-09-06 sprint 007 DO-3 刪兩項，不是重排。**理由：兩者都卡在未拍板的前提上，
且沒有任何 sprint 排了去解決那些前提（007-spec DO-3.3；同見 004-spec「本輪明確不做」）。

- **脈絡生命週期狀態機／burst detection** — 階段 1 收斂時已看過：Kleinberg 有 `s` 與
  `gamma` 兩個可調把手（D10），`nmarinsek/burst_detection` 非 OSI，MIT 替代等同未維護。
  「誰來決定 s 與 gamma」從未拍板。刪。真的重要它會自己再冒出來。
- **PTT 每日掃描** — 採用前須確認 PTT 使用條款（D13）；每日自動掃描即是排程（D7）。
  兩個前提都沒人排去解決。刪。

| 項目 | 層次 | 備註 |
|---|---|---|
| 深層挖掘：僅在未匹配爆發時觸發 | L2 | 股癌逐字稿深挖不排程，由使用者丟稿觸發。原先綁 burst；burst 已刪，本列只剩「人丟稿」那半 |
| 「太早」vs「吵完了」的區分 | L1+L2 | 2×2 中同屬「有人講但數字不動」那格。討論量那一軸隨 burst／PTT 刪除而暫停 |

### Sprint 007 明確不做（轉入 backlog，附物證）

007-spec「本輪明確不做」五項。刪不是這裡的工作——這五項是本輪故意不碰、要留著下次挑的。
每項附 spec／review 裡的物證，不只一行標題。

| 項目 | 層次 | 物證 |
|---|---|---|
| momentum 標籤重做 | 使用 | **階段 1：**全同向的日子無資訊；`Rank #1 · Δ5 +1 · 20D +26.5%` 卻標 `⚠️ Weakening`。同一現象在 `docs/sprints/past-momentum-visibility.md` 已出現過（Optical/CPO +26.5% 20D、-5.6% 1D → Weakening）。007 本輪動了顯示層，故意不順手改 momentum——改了會讓驗收條件 2（關掉新欄位後與 `dev` 逐字元相同）說不清楚是誰造成的 |
| 品質行可讀性重寫 | 使用 | **階段 1「只看輸出」角色的逐條疑問：**`.11 / .06±.03 / +2.8σ / 0/1000` 之間的關係看不出來、`換手 10 (80%)` 的 80% 是什麼、`D6` 在畫面上無對應。007 本輪不重寫品質行，理由同上（驗收條件 2） |
| `narrate add` 互動指令、速記檔入口 | L2 | 007 PO 前提 4：寫入摩擦本輪不碰。先確認回饋迴路有效（本輪 DO-1／DO-2），再降寫入摩擦——反過來做的話，摩擦降了但寫完還是沒反應。排下一輪 |
| 窮舉取代虛無抽樣；`n_iter` 一名兩義；guard-fail 格的 `n=` 位置 | ENG | **006-review：**A1（`0/1000` 實際只有 288 個相異位移，顯示精度超過設計能給的；窮舉更便宜）；A2（同一欄三格抽到同一組 1000 個 e，跨格 σ 誤差耦合；窮舉會讓 A2 消失）；小事（`n=342` 站在 guard-fail 格第二子行，橫著掃會看成虛無用了 342 天；`n_iter` DataFrame 欄是 `len(valid_null)`、CLI 尾行是使用者參數）。007 不可碰 `persistence_null_test`／`rank_ic_null_test`，留給之後 |
| 換 primary、composite、Elo、RRG、as-of 成分、雙池、burst、PTT | mixed | 007 本輪明確不做。R1 仍擋 composite；D3 仍擋 RRG；as-of 成分仍在待排（D6／Q5）；雙池是 open-questions Q6，004 用「籃子只並列」繞開而非回答。burst／PTT 見上，007 DO-3.3 已從待排刪除 |

### Sprint 001 發散階段產出、本輪未採用但值得留著

| 項目 | 層次 | 備註 |
|---|---|---|
| 主題間平均兩兩相關係數 | L1 | 相關性飆高＝「全市場同一筆交易」，排名當日無意義 |
| 重疊調整後的 RS20 | L1 | 剝除跨主題共用成分股的貢獻，檢查排名是否只是「AI 股被算三次」 |
| 中位數成員 RS20 vs 主題聚合 RS20 的落差 | L1 | 標示由一兩檔權值股扛起來的主題 |
| value_share 五日斜率（資金輪動速度） | L1 | 成交值佔比可能先於價格移動 |
| 「量先於價」個股偵測 | L1 | 成交量暴增但價格未反應 |
| 連續在榜天數計數 | L1 | 持續力的直觀提示，非分數 |
| 敘事提及次數的一階／二階導數 | L2 | 需先有脈絡記錄累積 |
| 敘事提及量 × RS20 的落後互相關（lag −10 到 +10） | L2 | 「對這個主題而言，話語領先還是落後價格」的實證讀數 |
| 「沉默但在動」象限圖 | L1+L2 | 2×2 的視覺化，需兩層都就緒 |
| 昨日排名的淡灰殘影疊在今日圖層上 | 呈現 | 位移量本身即是變化 |
| 每日 sparkline 小倍數取代單張 timeline | 呈現 | 橫向捲動看 90 天 |
| 缺值主題用虛線框而非補值 | 呈現 | 誠實呈現資料品質 |
| 「上次打開是幾天前」與期間摘要 | 呈現 | 避免逐日重播 |
| 每週五自填「下週誰會強」，隔週回填實際結果 | L2 | 使用者自己的判斷也是一種帶日期的敘事 |

### 資料現況（2026-09-06 複驗，取代 2026-09-05 那張表）

```text
twse   441 檔   20241227 → 20260904
tpex   441 檔   20241227 → 20260904      ← 日期集合完全相同，twse-only 0 / tpex-only 0
```

**更正：TPEx 那 89 天早在 sprint 004 就補齊了**（`004-evidence.md` 第 79–90 行，
2026-09-05 16:19）。本節上一版寫「tpex 351 檔／少 89 天」，是同日早上寫的，沒有跟上。
**010 的拍板 5 與 010-report 第 7 節第 3 項據此把它排進 011，那一格指向的是已完成的工作。**

**仍然是壞的：`data/raw` 有 33 個空日期，而「官方休市」與「抓取失敗」在磁碟上長得一樣。**
已確認 `2026-09-01`（週二、不在證交所休市清單上）被永久凍結成假日，
11 個族群的 rank timeline 全部少這一天。已排 011 DO-2。物證見 `011-evidence.md` 第 3 節。

**這一節之後不要再手寫。**事實應該由現場產生，文件只留判斷與理由——
本節連續兩輪被當成現場讀，兩輪都是錯的。

## 已完成

| 項目 | Sprint |
|---|---|
| TPEx 89 天回補（**更正：實際完成於 004，非 010／011**） | 004 |
| 品質行改寫成讀得懂的（百分位寫明窗口與方向、† 補註腳、D6 補白話、`entry is None` 印無基準） | 010 |
| `radar.html` 標頭補新鮮度（複用 `format_ops_status`；**但問錯了問題，見待排**） | 010 |
| 2×2「被講過」定義與 `敘事` 欄統一；`尚未生效`／`最近事件` 補溢出提示 | 010 |
| F4 測試名實不符、pending 真實目錄測試脆弱（009 驗收拍板 2／3） | 009 |
| `above_count` 全 NaN → NaN；空 `above_cols` → NaN | 005–006 |
| 虛無基準放在持續性數字旁（σ＋超越計數） | 003–004 |
| `calc.py` 資料空窗檢查（日期連續＋TWSE/TPEx 成對） | 004 |
| `reports/radar.html` 取消追蹤 | 003 |
| narrative schema branches/log/stage/revisit | 004 |
| 支線籃子強弱面板（並列不進池） | 004 |
| 持續性顯示改 σ＋超越計數；樣本窗不符當 absent | 004 |
| B＋C 讀法寫進 CLAUDE／README／design | 004 |
| 文件整地：分離已完成的建置期指令 | 000 |
| breadth 的 NaN 修正 | 001 |
| 訊號品質三數：持續性／換手率／離散度 | 001 |
| 故事層資料結構（帶日期 YAML + PIT-safe loader） | 001 |
| 多時間窗 RS（RS5/20/60）三個並列排名 | 002 |
| persistence 循環位移檢定 | 002 |

## 修訂紀錄

- 2026-09-07 sprint 012 收斂：三項進 DO（`Momentum` 欄寬、檔位表取代 `LIMIT_MOVE`、
  官方休市表）。**一則更正**——011 那 1640 筆偽陽性的成因不是檔位進位，是浮點比較，
  `calc.py:41-43` 的註解與 011-report §2.1 都要改（012 DO-2 一併處理）。
  未採用：裝 `exchange_calendars`（hardcode 假日只到 2026-02，會落後現實）、
  裝 bump chart 套件（matplotlib 薄包裝）、導入 `spec-kit`（第二個真理來源）。
  外部 review 的處方對照見 `docs/feedbacks/repo-review-response.md`。

- 2026-09-07 sprint 011 驗收：三項全數通過，未 merge。新增待排五項——除權息幅度量測
  （DO-1 用錯了尺，±10% 看不見 3–8%）、A4 改用實際漲跌停價、窮舉虛無（**現在有物證**：
  一天讓 k=1 從 +34.36σ 跳到 +39.41σ）、`shift(1)` 跨停牌空窗、`data/raw` 可驗證清單
  升格為前置。`Momentum` 欄寬那條由 009 舊項改註 011 惡化，排 012 第一格。

- 2026-09-06 sprint 011 收斂：三項進 DO。**兩則更正**——TPEx 89 天回補實際完成於 004
  （010 拍板 5 指向已完成的工作，該列刪除）；radar 新鮮度的立案物證是錯的
  （09-04 是週五），降級為 UNKNOWN，回頭日期 012 驗收後。
  「資料現況」表重寫並註明不要再手寫。新增待排五項（`data/raw` 可驗證清單、
  §27 月份贏家圖、`normalize_all` 全量重解、TWSE payload 無日期對帳、
  `date.today()` 用 UTC）。

- 2026-09-06 skill retro：新增「開發流程要不要再加 skill」盤點（結論：不加 skill，
  加一支驗收檢查腳本）。待排新增 `scripts/acceptance-check.sh`。
- 2026-09-06 sprint 010 驗收：三項全數通過，併入 `dev@874302d`，搬到「已完成」。
  新增待排四項——radar 新鮮度改「距今差幾個交易日」（DO-2 的洞，規劃端造成）、
  `DISPERSION_PCT_DIRECTION` 措辭、momentum 重做且必須可還原（D16）、
  TPEx 89 天回補（PO 拍板採讀法 A：補齊）。
- 2026-09-06 sprint 010 收斂：三項進 DO（全為顯示層）。待排移出「靜默截斷」與
  「`entry is None` 文案」（併入 010 DO-1／DO-3）。`arch` 那列補上查證結果（8.0.0、
  維護中、NCSA），押後理由改為「動 compute 層」而非「不確定可不可行」。
  未採用：主題間兩兩相關係數新指標（研究推翻，見 010-report 第 2 節）。
- 2026-09-06 sprint 009 併入 `dev@566ba37` 後：拍板 2／3 兩條測試項已於 `98e7198` 完工，搬到「已完成」。`Momentum` 欄寬、靜默截斷、`entry is None` 文案、gazetteer 四項仍在待排。
- 2026-09-06 sprint 009 驗收：新增六項（Momentum 欄寬、F4 測試名實不符、pending 真實目錄測試脆弱、靜默截斷、`entry is None` 文案、gazetteer／`narrate add` 押後）。前四項來自 009 驗收物證，見 `docs/sprints/009-report.md`。
- 2026-09-06 sprint 007：刪 `above_count` 待排殘留（005/006 已完工）、脈絡生命週期狀態機／burst、PTT 每日掃描；新增「007 明確不做」五項（momentum 標籤、品質行、`narrate add`、虛無窮舉／`n_iter`／`n=` 位置、換 primary 等），各附 spec／006-review 物證。
- 2026-09-05 review：001/002 已完成項目搬出待排（先前漏搬）；新增三項候選；
  記錄 ENG 永遠擠不進 DO 的結構偏差。
- 2026-09-05 sprint 002 收斂：多時間窗 RS 解除押後並進入 DO；新增 persistence 檢定。
- 2026-09-04 sprint 001 收斂：三項進入 DO；發散階段共 82 個候選，未採用但值得留的 15 項另列。
- 2026-09-04 建立（sprint 001 bootstrap）。種子來源：2026-09-04 技術債盤點與產品討論。
