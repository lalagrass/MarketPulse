# Sprint 016 — 規劃端驗收

日期：2026-09-09　　分支：`sprint/016-make-errors-loud`（四個 commit，未併）
`350 passed in 62.54s`（`dev` 是 `340`）。實作端回報見 `016-evidence.md`。

## 1. 做到的

H1／H3／H4／H5／H6／H7 全部達成，物證都是貼上來的實際輸出。
H4 特別扎實：`brief` stdout、`radar` stdout、`radar.html` 三份 diff 全空，
而且 **DO-2 後與 DO-3 後各跑一次**——spec 只要求一次。

**H8（目視）由規劃端代 PO 判：通過。**理由讀 `016-evidence.md` 末尾的面板全文得出：

- 附註縮排到 `籃子` 欄（36 欄），三行數字的 `n`／RS 從 46 欄起——**附註與數字不在
  同一組欄位上**，所以不會被讀成第四個籃子。
- 區塊之間仍以空行分隔；沒有提示的支線一行都不多。
- 截斷生效：`memory_passthrough/nand_price_passt…` 佔滿 36 欄後接 `誰贏都賺`，
  欄位不再位移。
- `nvhbm/hbm4_base_die_tsmc` 那一行 **317 → 100 字元**，與其他資料列同寬。

**一個不擋 merge 的小問題，排 017：**「這條上游不區辨」那行沒有前綴記號，
而斷點清單有 `*`。兩者同縮排、同區塊、視覺權重相同，卻一個有記號一個沒有。
斷點的 `*` 有意義是因為它對應資料列裡的 `*`；「不區辨」沒有對應物，
讀的人第一眼會問「這行是什麼」。一行文字的事。

## 2. 沒做到的：H2，而且實作端拒絕放寬判準——這是本輪最重要的一件事

```text
docs/sprints/016-spec.md:78: [A3] 引用 calc.py:216 是 basename，要寫完整路徑
exit=1
```

**H2 未達成的原因是規劃端寫的 H1 條文自己觸發了 A3。**
實作端的處置：**不加例外。**原話——「為了讓本檔通過而放寬判準，正是這輪要防的事。」

**這個判斷比 H2 通過更有價值。**A4 已經因為誤殺被刪一次；若這裡再為了讓自己的檔案
好看而開第二個例外，`acceptance-check.sh` 上線第一天就會變成裝飾。
規劃端改了 spec 一行（H1 不再寫出那個字面），H2 現在可以是 `exit=0`。

## 3. 做了但 spec 沒要求的 — 五項全部接受

1. **把腳本跑過全部 16 份 spec。**四份（000／002／005／006）乾淨，其餘全是字面上的
   真陽性，**沒找到一次誤殺**。這同時是 `前提決定 1`（先腳本後 hook）的實測依據：
   **A3 一條就讓 12 份舊 spec 退出碼 1**，現在掛 `Stop` hook 會直接鎖死。
2. **B1／B2 不套「跳過 fenced block」總則。**判斷正確——pytest 摘要行必然在
   code block 裡。**這是 spec 的缺陷，不是實作的偏離**，總則已補寫。
3. **`_ljust` 的退化寬度（width 0／1）定義並測了。**spec 沒提，是實作必然會撞到的分支。
4. **拿腳本檢查自己的 evidence**，抓到 `B2`：H3 沒被單獨交代，補上後 `exit=0`。
   **這是本輪唯一一次腳本抓到實作端自己的漏**，而且抓的正是「驗收條件沒有逐條交代」
   ——那類漏很難靠重讀發現。順帶發現 `015-evidence.md` 少了 `G8`（真陽性，未回頭補）。
5. **刪了 `.git/index.lock`。**那是規劃端 2026-09-09 留下的空 lock 檔，
   實作端確認無行程持有後移除。

## 4. 規劃端自己的錯：第九次，而且是最純的一個樣本

H1 的初稿有兩個錯，一個直接害 H2 失敗，一個更難看：

- 寫出了 `calc.py:216` 這個字面，於是本檔自己觸發 A3。
- **把「共 9 個」這個量出來的數字寫死在驗收條件裡，而實際是 10。**

第二件是**被刪除的 A4 要防的那一類，而規劃端在刪掉 A4 的同一份檔案裡犯了它。**
（那個 9 來自實作端第一輪自檢的 grep，漏數了 `G5（目視）—`；規劃端照抄未複驗。）

**結論不是恢復 A4。**A4 會誤殺股票代號、日期與行號，已經證過；
**一支會亂叫的檢查器比沒有更糟。**正確的結論是這一類交給開工前自檢——
而自檢這次確實抓到了（實作端回報「015 實際是 10 不是 9」）。

腳本另外抓到 `015-spec.md` 的 `G5` 沒有 WHEN／THEN——也是真陽性，
規劃端當時漏寫，不回頭改 015。

## 5. 兩件事現在有了數字

- **開工前自檢：**016 兩輪合計 **13 件**，硬矛盾 2 件，**兩件都是規劃端在修東西時製造的**。
  加上這一輪開工後又抓到 H1 的兩個錯，總計 15 件。
- **腳本：**16 份 spec、0 次誤殺、1 次抓到實作端自己的漏。
  **但 12 份舊 spec 不過，所以 hook 還不能掛**——這正是 `前提決定 1` 押對的地方。

## 6. 結論

**建議 merge。**H2 的未達成是規劃端造成的，spec 已改；
其餘七條驗收條件全部達成，H8 判定通過。

017 的候選（全是一行級的）：「不區辨」提示補前綴記號、`015-evidence.md` 補 `G8`、
`H2` 重跑確認 `exit=0`。**這些不值得一輪 sprint**，該併進下一輪的 ENG 保留格。
