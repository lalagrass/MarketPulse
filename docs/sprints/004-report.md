# Sprint 004 — 報告（階段 0 驗收，寫於 005 開場）

狀態：驗收完成 2026-09-06
驗收者：PO（規劃端代寫；物證來自 `004-evidence.md` 與已併入 `dev` 的 diff）
主幹 tip：`42c3522`（`origin/dev`）

## 上輪做了什麼（對照 spec）

### 做到的

| DO | 判定 | 物證 |
|---|---|---|
| DO-3 資料空窗檢查 | 通過 | commit `9814796`；evidence 驗收 1–3（raise 訊息含空窗／TPEx 缺 89 天；補齊後 analyze 恢復） |
| DO-1 narrative schema | 通過 | commit `8e0dfc1`；`2026-09-04.yaml` 仍可載；`2026-09-06.yaml` 真實樣本；history／revisit／bears_on 測試 |
| DO-2 支線籃子面板 | 通過 | commit `9849b26`；brief/radar 逐字元不變（R3）；as-of 成員；n/a／空籃子列 |
| 第 4 步回報（非驗收） | 完成 | k=1/5/20 虛無在拉長樣本上全過半圈護欄；第二層閘門打開 |

### 沒做到的

- 無。spec 三項 DO 皆有對應 commit 與 evidence 字串。
- `004-spec` 狀態曾停在「待實作」直到文件收尾才改「已完成」——流程缺口，不是功能缺口。

### 做了但 spec 沒要求的（第三類）

1. **顯示層改 σ＋超越計數、樣本窗不符當 absent**（`3d812c4`、`095f79c`）— PO 追加授權，evidence 有註記；`quality.py` 原在不可碰清單。
2. **三份文件 B＋C 讀法**（`42c3522`）— merge 前把過期「1.0σ／未達門檻」敘述換成 0.14／+2.84σ／0/1000。
3. **規劃端前提寫錯兩次**（003 分支已併）— 實作端擋下；spec 內已明文更正。這是規劃端自己造成的問題。

## 測試

規劃端不跑測試。唯一來源：`004-evidence.md` 寫 131 passed（追加後）。本驗收**不重跑**、不聲稱本地通過。

## 使用痕跡（§26）

`narratives/2026-09-06.yaml` 存在——EP694 寫成 asic_xpu 的 log＋兩條支線。  
**解讀：**第二層 schema 有被用過一次。第一層 brief/radar 的日常使用仍無新快照以外的痕跡。未達「連續兩輪完全沒用」的強制 ENG-only 門檻，但 005 仍應保留「使用／ENG」格。

## 未決（帶進 005）

- Q5／Q6 仍開（as-of 成分、雙池）— 004 用「籃子並列不進池」繞開，未結案。
- Q8 merge 權限 — 本輪 `dev` push 由使用者授權的本機操作完成；規則帳仍待正式寫。
- backlog「待排」仍掛已完工項（虛無基準旁、空窗檢查、radar.html 追蹤等）— **文件負債**，005 要掃。

## 決定（供 005 開場）

1. 004 驗收通過；`dev@42c3522` 為基準。
2. k=20 讀法鎖定 B＋C（文件已寫）；不重開「換 rank」。
3. 下一輪優先：**把尺度敘事變成可複核物證**（Rank-IC），並清 backlog 假待排。
