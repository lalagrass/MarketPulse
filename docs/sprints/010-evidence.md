# Sprint 010 — Evidence

Branch: `sprint/010-say-what-we-know` (from `dev@73faf49`, which is
`c673700` + the 010 spec commit; **not merged**).
As of: 2026-09-06. Every `refresh` below was run as
`uv run marketpulse refresh --end 2026-09-04`, so `lo > hi` and it prints
`already current through 2026-09-04; skip download` — **not one byte of
`data/raw` was fetched** (兔子洞).

## Commits (full hashes)

| DO | Hash | Subject |
|---|---|---|
| DO-1 | `adf94e1ee36e9742b0bbd956855881f273e07b65` | feat(quality): DO-1 rewrite the quality line to read (display only) |
| DO-2 | `ed76f1fa81e30ec606e13c9649dc16d6254723a3` | feat(radar): DO-2 put data freshness in radar.html |
| DO-3 | `bc0b3d03e289f671c18725d11f1746f1e33588a0` | feat(narratives): DO-3 wire up two layer-2 things already computed but silent |

## `uv run pytest` — actual summary line

```text
228 passed in 46.40s
```

Command: `UV_CACHE_DIR=$PWD/.uv-cache uv run pytest` (after `uv sync --extra dev`).
Baseline on `dev` was `213 passed`; +15 new tests (A1–A3 + 4 DO-1 detail,
B1–B3, C1–C4 + one split into two).

## `git diff dev --stat`

```text
 marketpulse/cli.py        |  50 +++++++++++----
 marketpulse/narratives.py |  23 +++++--
 marketpulse/product.py    |   9 +++
 marketpulse/quality.py    | 120 ++++++++++++++++++++++++++---------
 marketpulse/radar.py      |  39 +++++++++++-
 tests/test_narratives.py  | 135 +++++++++++++++++++++++++++++++++++++++
 tests/test_product.py     |  66 ++++++++++++++++++++
 tests/test_quality.py     | 156 +++++++++++++++++++++++++++++++++++++++++++++-
 tests/test_radar.py       |  94 ++++++++++++++++++++++++++++
 9 files changed, 643 insertions(+), 49 deletions(-)
```

Files **not** touched: `calc.py`, `momentum.py`, `themes/v1.yaml`,
`narratives/*.yaml`, `data/**`, `docs/product/**`. `git diff dev -- marketpulse/calc.py marketpulse/momentum.py` is empty.

## Compute layer unchanged (A3 / C4)

Function-body comparison, `dev` vs `HEAD`, via `ast.get_source_segment`:

```text
compute_market_quality       : IDENTICAL
_percentile                  : IDENTICAL
persistence_null_test        : IDENTICAL
load_as_of                   : IDENTICAL
theme_mention_dates          : IDENTICAL
theme_last_mention_dates     : IDENTICAL
weak_rank_threshold          : IDENTICAL
out_of_classification_symbols: IDENTICAL
```

`market_daily.parquet` / `theme_daily.parquet` regenerated by
`marketpulse analyze` on `dev`'s `marketpulse/` and on this branch's, then
content-hashed with `hashlib.sha256(pd.util.hash_pandas_object(df, index=True))`:

```text
                 rows   content_sha256 (dev)              content_sha256 (010)
theme_daily      4488   eb95152e6a3484cbb23e345d44de2df2be9602d0dfeec32da926e131ebe340fd   (same)
market_daily      388   722d5a0caa14e34782321ea695b6a843ce0aa422f8c62649b34ba4b782d09d92   (same)
```

Byte-for-byte identical schema and content.

---

## DO-1 — quality line rewritten

### A1 (可測) — `entry is None` prints `無基準`, no persistence digit

`quality_line(row, null_baseline=None)` line 0:

```text
持續性 無基準
```

`not any(ch.isdigit() for ch in first_line)` holds; the fixture's daily
`rank_persistence_20` digit (`-1.00`) does not appear anywhere in the
string. A baseline with a rejected `method_version` routes through the same
path and also prints `持續性 無基準`.
Test: `tests/test_quality.py::test_do1_a1_entry_none_prints_no_baseline_not_a_persistence_digit`.

### A2 (可測) — `market_row=None` unchanged

`quality_line(None)` returns exactly `持續性 n/a   換手 n/a   離散 n/a`
(one line, no `\n`, no HORIZON_FOOTNOTE, no D6 gloss), including when a
null baseline is passed.
Test: `tests/test_quality.py::test_do1_a2_missing_market_row_unchanged_by_this_sprint`.

### A3 (可測) — display only

`compute_market_quality` still returns `MARKET_COLUMNS` with
`dispersion_pct` NaN through the 60-session warm-up, and `quality_line`
echoes a hand-built `market_row` verbatim (`換手 7（…第 42 百分位…）`,
`離散 12.3pp（…第 7 百分位…）`) — it does not recompute.
Test: `tests/test_quality.py::test_do1_a3_display_only_compute_layer_output_unchanged`.
Plus the function-body + parquet-hash comparison above.

### A4 (目視驗證 — PO 判讀，測試不是證據)

**One physical line vs one group per line — both from the same real
`refresh` (open question 1). PO picks; default shipped is expanded.**

**Expanded (shipped default):**

```text
持續性 .14† 虛無 .06±.03 +2.8σ 0/1000
換手 12（近 60 個交易日第 90 百分位；越高＝名次相對前一交易日洗牌越多）
離散 15.8pp（近 60 個交易日第 18 百分位；越低＝族群名次越擠、當日名次差異的資訊量越低）
† 持續性這一組（觀測值與虛無、σ、超出次數）量到 2026-09-03（baseline sample_end）為止，不是今天
月尺度：可偵測≠穩定；短端遠強於長端；凍結成分偏高估（D6）。
D6：持續性是在凍結成分股（非 as-of 成分）上量的，這個偏誤的方向是高估。
```

**One line (`quality_line(..., oneline=True)`):**

```text
持續性 .14† 虛無 .06±.03 +2.8σ 0/1000   換手 12（近 60 個交易日第 90 百分位；越高＝名次相對前一交易日洗牌越多）   離散 15.8pp（近 60 個交易日第 18 百分位；越低＝族群名次越擠、當日名次差異的資訊量越低）
† 持續性這一組（觀測值與虛無、σ、超出次數）量到 2026-09-03（baseline sample_end）為止，不是今天
月尺度：可偵測≠穩定；短端遠強於長端；凍結成分偏高估（D6）。
D6：持續性是在凍結成分股（非 as-of 成分）上量的，這個偏誤的方向是高估。
```

**這一行現在在說什麼（一句）：** 三個尺度分開報 —— 20 日排名持續性 0.14，其
循環位移虛無是 0.06±0.03，觀測值高出 2.8σ 且 1000 次位移沒有一次追上（`†`：這組是量
到 2024-12-27→2026-09-03，不是今天 09-04）；名次相對前一交易日的變動量今天落在近
60 個交易日的第 90 百分位（洗牌偏多）；族群之間的名次差距今天落在近 60 個交易日的
第 18 百分位（偏擠，當日名次的資訊量偏低）。無門檻、無判語。

Changes vs `dev`, whole brief, are **only these six lines** (`diff`):

```diff
< 持續性 .14† 虛無 .06±.03 +2.8σ 0/1000   換手 12 (90%)   離散 15.8pp (18%)
---
> 持續性 .14† 虛無 .06±.03 +2.8σ 0/1000
> 換手 12（近 60 個交易日第 90 百分位；越高＝名次相對前一交易日洗牌越多）
> 離散 15.8pp（近 60 個交易日第 18 百分位；越低＝族群名次越擠、當日名次差異的資訊量越低）
> † 持續性這一組（觀測值與虛無、σ、超出次數）量到 2026-09-03（baseline sample_end）為止，不是今天
3a7
> D6：持續性是在凍結成分股（非 as-of 成分）上量的，這個偏誤的方向是高估。
```

Everything else in `brief` is byte-identical to `dev`.

---

## DO-2 — data freshness in `radar.html`

### B1 (可測) — page carries `raw last attempt` + per-market status

`render_radar_html(..., freshness=OpsStatus(attempt={date:2026-09-04,
twse:ok, tpex:empty, usable:False}, as_of=…, caught_up=False))` contains:

```text
raw last attempt 2026-09-04
twse=ok
tpex=empty
資料到 <as_of>
```

Test: `tests/test_radar.py::test_b1_radar_html_carries_last_raw_attempt_date_and_per_market_status`.

### B2 (可測) — behind wording gated on `caught_up`; `freshness=None` a no-op

- `caught_up=False` → page has `尚未追上`
- `caught_up=True` → page does **not** have `尚未追上`; both carry
  `raw last attempt 2026-09-04`
- `render_radar_html(...)` == `render_radar_html(..., freshness=None)`, and
  neither contains `raw last attempt` — the header is byte-identical to
  `dev` when no freshness is supplied.

Test: `tests/test_radar.py::test_b2_radar_html_behind_wording_only_when_not_caught_up`.

### B3 (可測) — `format_ops_status()` terminal string byte-for-byte unchanged

`uv run marketpulse chart` tail, `dev` vs branch — `diff` is empty:

```text
raw last attempt: 2026-09-04  twse=ok  tpex=ok
bars/snapshot as_of: 2026-09-04
chart: reports/rotation_latest.png  effective 2026-07-09 → 2026-09-04
```

`format_ops_status` now formats from `cli.ops_status() -> OpsStatus`; the
three legacy shapes (caught-up, `(will retry)`, `raw last attempt: none`)
are pinned through the formatting half in
`tests/test_radar.py::test_b3_format_ops_status_terminal_string_byte_for_byte`.

### B4 (目視驗證 — PO 判讀，測試不是證據)

Actual `<header>` of `reports/radar.html` from the real `refresh` (this
machine: attempt 09-04 == as_of 09-04, so `caught_up` is True — no behind
wording, which is B2's `True` case):

```html
<header id="top">
  <h1>MarketPulse</h1>
  <p class="sub">Sector Rotation · 2026-09-04</p>
  <p class="sub freshness">資料到 2026-09-04 · raw last attempt 2026-09-04（twse=ok tpex=ok）</p>
  <p class="sub quality">持續性 .14† 虛無 .06±.03 +2.8σ 0/1000
換手 12（近 60 個交易日第 90 百分位；越高＝名次相對前一交易日洗牌越多）
…</p>
  …
</header>
```

CSS added: `.freshness { color: #444; margin: 0 0 10px; font-size: 0.85rem; }`.

**The freshness line string, all four states** (`radar._freshness_line`):

```text
caught_up=True : 資料到 2026-09-04 · raw last attempt 2026-09-04（twse=ok tpex=ok）
caught_up=False: 資料到 2026-09-04 · raw last attempt 2026-09-04（twse=ok tpex=empty） · 尚未追上，refresh 會重試
attempt=None   : 資料到 2026-09-04 · raw last attempt: none
freshness=None : (line omitted; header byte-identical to dev)
```

**Open question 2 — placement. Three candidates, same fact string:**

1. **Its own sub-line under the date (SHIPPED):**
   ```html
   <p class="sub">Sector Rotation · 2026-09-04</p>
   <p class="sub freshness">資料到 2026-09-04 · raw last attempt 2026-09-04（twse=ok tpex=ok）</p>
   <p class="sub quality">持續性 .14† …</p>
   ```
2. **Appended to the date line:**
   ```html
   <p class="sub">Sector Rotation · 2026-09-04　·　資料到 2026-09-04 · raw last attempt 2026-09-04（twse=ok tpex=ok）</p>
   ```
3. **A distinct bar above `<h1>`:**
   ```html
   <p class="sub freshness" style="border-bottom:1px solid #e0e0e0;padding-bottom:6px">
     資料到 2026-09-04 · raw last attempt 2026-09-04（twse=ok tpex=ok）
   </p>
   <h1>MarketPulse</h1>
   ```

Shipped #1: cheapest, no `<h1>`/`<title>` change, no `_vislen` guard near it
(那條護欄是文字表頭的，不是 HTML 的). PO judges "打開來一眼知不知道資料到哪天".

---

## DO-3 — two silent layer-2 things wired up

### C5 (目視驗證) — real `refresh` 2×2 + pending/recent, **vs `dev`**

Full `brief` output compared `dev` vs branch: **the only difference in the
entire brief is the DO-1 quality line** (the six lines shown in A4). The
DO-3 surfaces are **byte-identical to `dev` today**:

```text
強但沒人講
光通訊/CPO  #1
散熱/液冷  #2
高速材料/CCL  #3

有人講但弱
（無）

分類外代號
asic_xpu · 2454

尚未生效
2026-09-06 · 2026-09-06.yaml
（快照日期晚於最新價量日 2026-09-04，依 PIT 規則尚未納入）
…
最近事件
（無）
```

This is expected and was stated in the spec: DO-3.1's visible effect today
is zero (two snapshots, both "被講過" definitions agree), and DO-3.2's
overflow lines do not fire (1 pending snapshot, 0 events — both ≤ cap 3).

### C1 (可測) — 有人講但弱 uses the every-snapshot mention scan

Constructed fixture: theme `t11` (rank 11, `weak_rank_threshold(11)==6`),
`overlay.snapshot` = latest PIT snapshot with **no** narratives,
`overlay.mention_dates = {"t11": 2026-09-01}` (an earlier snapshot named it).

```text
with overlay.mention_dates   → 有人講但弱 contains  "主題十一  #11"
without (mention_dates=None)  → 有人講但弱 does NOT contain it   (== dev behaviour)
```

Test: `tests/test_product.py::test_c1_covered_weak_uses_every_snapshot_mention_not_just_current_pit`.

### C2 (可測) — `mention_dates=None` byte-identical to `dev`

`_mention_lookup(overlay)` with `overlay.mention_dates is None` returns
exactly `theme_mention_dates(overlay.snapshot, overlay.themes)` (asserted
equal), and the rendered gap lists match the pre-change single-snapshot
behaviour.
Test: `tests/test_product.py::test_c2_mention_dates_none_falls_back_to_single_pit_snapshot`.

### C3 (可測) — overflow lines past the cap

`尚未生效`: exactly 3 pending files → 3 rows, no `還有` line. A 4th file →
still 3 rows (cap unchanged) + `還有 1 份`. The three shown are the oldest
(newest-not-required, unchanged).
`最近事件`: exactly 3 `log` entries → 3 rows, no `還有` line. A 4th →
3 rows + `還有 1 筆`.
Constants: `PENDING_OVERFLOW_NOTE = "還有 {n} 份"`,
`RECENT_EVENTS_OVERFLOW_NOTE = "還有 {n} 筆"`. Caps `PENDING_LIMIT` /
`RECENT_EVENTS_LIMIT` unchanged at 3.
Tests: `tests/test_narratives.py::test_c3_pending_snapshots_overflow_line_beyond_the_cap`,
`::test_c3_recent_events_overflow_line_beyond_the_cap`.

### C4 (可測) — five pinned functions untouched

`weak_rank_threshold(0,1,10,11) == [0,1,5,6]`; `load_as_of` picks the
latest snapshot and trims; `theme_mention_dates` on the latest PIT snapshot
returns `None` for a theme only named earlier while
`theme_last_mention_dates` returns that earlier date;
`out_of_classification_symbols` still lists `("x","ZZZ")`.
Test: `tests/test_narratives.py::test_c4_do3_2_leaves_the_five_pinned_functions_untouched`.
Plus the `ast` body comparison above.

---

## Open questions — measured, not decided

### 1. 品質行一行還是多行？

**Measured, both rendered above (A4).** Expanded is 6 lines and each
percentile's window/direction sits on its own line where it is easy to
read; one-line is one ~150-char metric line + 3 footnote lines and needs a
wide terminal / wraps in `radar.html`. **Shipped: expanded** (spec 拍板 1
default). `quality_line(oneline=True)` is a one-arg flip if the PO wants
compact — layout only, no semantics.
**Recommendation: keep expanded.**

### 2. `radar.html` 新鮮度放哪裡？

**Measured, three placements rendered above (B4).** Shipped: its own
`<p class="sub freshness">` between the "Sector Rotation · date" line and
the quality block. It needs no `<h1>`/`<title>` edit and does not go near
the `_vislen` text-table guard.
**Recommendation: keep #1 (own sub-line).**

### 3. `format_ops_status` 要不要拆成資料結構？

**Done — split.** New `cli.OpsStatus` (frozen: `attempt`, `as_of`,
`caught_up`) + `cli.ops_status(data_dir)`. `format_ops_status` formats from
it; its terminal string is byte-for-byte unchanged (B3, `diff` empty).
`render_radar_html` renders its own phrasing from the same object, so the
page and the terminal state one fact set. The split was clean and B3 held,
so per the spec's "拆得乾淨且 B3 保得住就拆" — **split.**
